# Номер сборки берётся из CI_BUILD_NUMBER (автоинкремент в Appflow)
vars:
  CI_BUILD_NUMBER:
    default: 1
platforms:
  ios:
    buildNumber: $CI_BUILD_NUMBER
  android:
    versionCode: $CI_BUILD_NUMBER
